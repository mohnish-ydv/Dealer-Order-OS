# Copy to keystore.properties for a local signed release build.
# Never commit the real keystore.properties or the .jks file.
storeFile=/absolute/path/to/apex-dms-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=apex_dms_release
keyPassword=YOUR_KEY_PASSWORD
